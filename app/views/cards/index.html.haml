.card-wrapper
  = render 'shared/header-main'
  .cardIndex
    .mypageMenu
      =render 'shared/menu-mypage'
    .cardList
      =render 'cardList'
  = link_to new_card_path(current_user.id) do
    クレジットカード追加

